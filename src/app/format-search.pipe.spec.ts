import { FormatSearchPipe } from './format-search.pipe';

describe('FormatSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
